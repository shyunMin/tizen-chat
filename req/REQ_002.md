# REQ_002 — 듀얼 HTTP 입력 모드 (오버레이 + 프롬프트)

## 요구사항

첫 화면(`TizenChatScreen2`)에서 두 가지 HTTP 입력 모드를 동시에 지원한다:

| 모드 | 진입 조건 | HTTP 메시지 라우팅 |
|-----|-----------|-------------------|
| **오버레이 모드** | ArrowUp 키 → `HttpMessageOverlayScreen` push | 화면 중앙 텍스트로 표시 |
| **프롬프트 모드** | PromptBar가 열려 있는 상태 | `_handleSend()` 호출 |

두 모드는 배타적으로 동작한다 (오버레이가 떠 있는 동안 프롬프트로 전달 안 함).  
오버레이 모드는 나중에 다른 뷰로 확장 가능하도록 설계한다.

---

## 설계: HttpMessageBus (싱글턴)

포트 충돌을 막고 확장성을 확보하기 위해 단일 서버 인스턴스를 공유하는  
`HttpMessageBus` 싱글턴을 도입한다.

```
HttpMessageBus (singleton)
├── HttpMessageServer 내부 소유 (포트 7777, 앱 생애주기와 동일)
├── broadcast Stream<String> 제공
└── acquire() / release() 로 참조 카운팅 → 마지막 release 시 서버 종료

TizenChatScreen2
├── initState: bus.acquire() → 구독 (guard: _isVisible && !_isWaiting && !_isOverlayActive)
├── ArrowUp: _isOverlayActive = true → push HttpMessageOverlayScreen
│            .then((_) → _isOverlayActive = false)
└── dispose: 구독 취소 + bus.release()

HttpMessageOverlayScreen
├── initState: bus.acquire() → 구독 (항상 수신 → UI 표시)
└── dispose: 구독 취소 + bus.release()
```

`_isOverlayActive` 플래그가 두 모드의 배타성을 보장한다.

---

## AI 구현 프롬프트

### PROMPT START

You are a Flutter developer on a Tizen-based Flutter project.

Implement a **dual HTTP input mode** system. The architecture is:
- A singleton `HttpMessageBus` owns the HTTP server and exposes a broadcast stream.
- `TizenChatScreen2` routes messages to `_handleSend()` only when PromptBar is visible and no overlay is active.
- `HttpMessageOverlayScreen` routes messages to a center text display while it is shown.

---

#### File 1 (NEW): `lib/features/http_message_overlay/http_message_bus.dart`

```dart
import 'dart:async';
import 'dart:convert';
import 'dart:io';

const int kHttpMessagePort = 7777;

/// Singleton HTTP message bus.
/// Manages a single HttpServer instance shared by all consumers.
/// Use acquire() before subscribing and release() when done.
class HttpMessageBus {
  HttpMessageBus._internal();
  static final HttpMessageBus instance = HttpMessageBus._internal();

  HttpServer? _server;
  int _acquireCount = 0;
  // Persistent controller — never closed so re-subscription always works.
  final StreamController<String> _controller =
      StreamController<String>.broadcast();

  /// Broadcast stream of incoming messages.
  Stream<String> get stream => _controller.stream;

  /// Call once per consumer (in initState). Starts server on first call.
  Future<void> acquire() async {
    _acquireCount++;
    if (_server != null) return; // already running
    try {
      _server = await HttpServer.bind(
          InternetAddress.loopbackIPv4, kHttpMessagePort);
      print('[HttpMessageBus] Listening on port $kHttpMessagePort');
      _server!.listen(_handleRequest);
    } catch (e) {
      print('[HttpMessageBus] Failed to start: $e');
    }
  }

  /// Call once per consumer (in dispose). Stops server when no consumers remain.
  Future<void> release() async {
    _acquireCount = (_acquireCount - 1).clamp(0, 9999);
    if (_acquireCount == 0) {
      await _server?.close(force: true);
      _server = null;
      print('[HttpMessageBus] Server stopped');
    }
  }

  Future<void> _handleRequest(HttpRequest request) async {
    request.response.headers.add('Access-Control-Allow-Origin', '*');
    request.response.headers
        .add('Access-Control-Allow-Methods', 'POST, OPTIONS');
    request.response.headers
        .add('Access-Control-Allow-Headers', 'Content-Type');

    if (request.method == 'OPTIONS') {
      request.response.statusCode = HttpStatus.ok;
      await request.response.close();
      return;
    }

    if (request.method == 'POST' && request.uri.path == '/message') {
      try {
        final body = await utf8.decoder.bind(request).join();
        String message = body;
        final ct = request.headers.contentType?.toString() ?? '';
        if (ct.contains('application/json')) {
          final data = jsonDecode(body);
          message = data['text']?.toString() ?? body;
        }
        _controller.add(message);
        request.response
          ..statusCode = HttpStatus.ok
          ..headers.contentType = ContentType.json
          ..write('{"status":"ok"}');
      } catch (e) {
        print('[HttpMessageBus] Request error: $e');
        request.response.statusCode = HttpStatus.internalServerError;
      }
    } else {
      request.response.statusCode = HttpStatus.notFound;
    }
    await request.response.close();
  }
}
```

---

#### File 2 (MODIFY): `lib/features/http_message_overlay/http_message_overlay_screen.dart`

Replace the existing `HttpMessageServer` usage with `HttpMessageBus`.

Full replacement content:

```dart
import 'dart:async';
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'http_message_bus.dart';

class HttpMessageOverlayScreen extends StatefulWidget {
  const HttpMessageOverlayScreen({super.key});

  @override
  State<HttpMessageOverlayScreen> createState() =>
      _HttpMessageOverlayScreenState();
}

class _HttpMessageOverlayScreenState extends State<HttpMessageOverlayScreen> {
  StreamSubscription<String>? _subscription;
  String _currentMessage = "HTTP 메시지를 기다리는 중...";

  @override
  void initState() {
    super.initState();
    HttpMessageBus.instance.acquire().then((_) {
      _subscription = HttpMessageBus.instance.stream.listen((msg) {
        if (mounted) {
          setState(() => _currentMessage = msg);
        }
      });
    });
  }

  @override
  void dispose() {
    _subscription?.cancel();
    HttpMessageBus.instance.release();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
      body: Focus(
        autofocus: true,
        onKeyEvent: (node, event) {
          if (event is KeyDownEvent &&
              (event.logicalKey == LogicalKeyboardKey.escape ||
                  event.logicalKey == LogicalKeyboardKey.goBack)) {
            Navigator.of(context).pop();
            return KeyEventResult.handled;
          }
          return KeyEventResult.ignored;
        },
        child: SizedBox.expand(
          child: Center(
            child: Container(
              padding:
                  const EdgeInsets.symmetric(horizontal: 40, vertical: 24),
              margin: const EdgeInsets.all(40),
              decoration: BoxDecoration(
                color: Colors.black.withOpacity(0.5),
                borderRadius: BorderRadius.circular(16),
                border: Border.all(color: Colors.white24),
              ),
              child: Text(
                _currentMessage,
                style: const TextStyle(
                  color: Colors.white,
                  fontSize: 36,
                  fontWeight: FontWeight.bold,
                ),
                textAlign: TextAlign.center,
              ),
            ),
          ),
        ),
      ),
    );
  }
}
```

---

#### File 3 (MODIFY): `lib/screens/tizen_chat_screen_2.dart`

Apply the following **surgical changes** to this file:

**3-1. Replace imports** for http:
```dart
// REMOVE:
import '../features/http_message_overlay/http_message_server.dart';

// ADD:
import '../features/http_message_overlay/http_message_bus.dart';
import '../features/http_message_overlay/http_message_overlay_screen.dart';
```
Keep `import 'dart:async';` as is.

**3-2. Replace fields** related to HttpMessageServer:
```dart
// REMOVE:
final HttpMessageServer _httpServer = HttpMessageServer();
StreamSubscription<String>? _httpSubscription;

// ADD:
StreamSubscription<String>? _httpSubscription;
bool _isOverlayActive = false; // true while HttpMessageOverlayScreen is shown
```

**3-3. Replace initState()** body (after super.initState() and _chatService.connect()):
```dart
// REMOVE the block:
_httpServer.start().then((_) {
  _httpSubscription = _httpServer.messageStream.listen((text) {
    if (mounted && !_isWaiting) {
      _handleSend(text);
    }
  });
});

// ADD:
HttpMessageBus.instance.acquire().then((_) {
  _httpSubscription = HttpMessageBus.instance.stream.listen((text) {
    // Prompt mode: only route to _handleSend when prompt is visible
    // and overlay is not active
    if (mounted && _isVisible && !_isWaiting && !_isOverlayActive) {
      _handleSend(text);
    }
  });
});
```

**3-4. Replace dispose()**:
```dart
// REMOVE:
_httpSubscription?.cancel();
_httpServer.stop();

// ADD:
_httpSubscription?.cancel();
HttpMessageBus.instance.release();
```
Keep `_keyboardFocusNode.dispose();` and `super.dispose();` as is.

**3-5. Add method `_pushHttpMessageOverlay()`** (add anywhere inside `_TizenChatScreen2State`, e.g. after `_toggleVisibility()`):
```dart
void _pushHttpMessageOverlay() {
  setState(() => _isOverlayActive = true);
  Navigator.of(context)
      .push(
        PageRouteBuilder(
          pageBuilder: (context, animation, secondaryAnimation) =>
              const HttpMessageOverlayScreen(),
          transitionDuration: Duration.zero,
          reverseTransitionDuration: Duration.zero,
        ),
      )
      .then((_) {
        if (mounted) setState(() => _isOverlayActive = false);
      });
}
```

**3-6. Add ArrowUp handler** inside `onKeyEvent`, BEFORE the existing Alt/Meta block:
```dart
// Arrow Up → Overlay mode
if (event is KeyDownEvent &&
    event.logicalKey == LogicalKeyboardKey.arrowUp) {
  if (!_isWaiting && !_isOverlayActive) {
    _pushHttpMessageOverlay();
  }
  return KeyEventResult.handled;
}
```

---

#### Constraints

- Do NOT modify `pubspec.yaml`.
- Do NOT modify `http_message_server.dart` (it can remain but is no longer used by these files).
- Do NOT add test files.
- `HttpMessageBus._controller` must NEVER be closed — it uses `broadcast()` and must be reusable across multiple `acquire/release` cycles.

### PROMPT END

---

## 검증 시나리오

### 시나리오 A — 오버레이 모드
1. 앱 실행 후 ArrowUp 키 입력 → `HttpMessageOverlayScreen` 등장
2. curl 명령 실행:
   ```bash
   curl -X POST http://localhost:7777/message \
     -H "Content-Type: application/json" \
     -d '{"text": "오버레이 메시지 테스트"}'
   ```
3. 화면 중앙 텍스트가 갱신되어야 함
4. Escape/Back 키로 오버레이 종료

### 시나리오 B — 프롬프트 모드
1. 앱 실행 후 Alt 키 입력 → PromptBar 등장
2. 동일한 curl 명령 실행
3. AI 응답 흐름이 시작되어야 함 (PromptBar 사라지고 응답 화면 push)

### 시나리오 C — 배타성 검증
1. 오버레이가 떠 있는 동안 curl 실행
2. 오버레이에서만 텍스트 표시, `_handleSend` 호출 없음을 확인
